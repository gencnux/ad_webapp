version: '3.9'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: powershell_web
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DJANGO_SETTINGS_MODULE=powershell_web.settings
    restart: always
